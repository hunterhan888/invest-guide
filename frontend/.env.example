# 基础 API 路径（前端 API 客户端相对路径，经 Vite 代理转发到后端）
VITE_API_BASE_URL=/api/v1

# Mock 开关：true 使用内置 mock（无需后端）；false 对接真实后端
VITE_USE_MOCK=true

# 仅开发模式生效：Vite dev server 将 /api/v1 代理到该后端地址
VITE_API_PROXY_TARGET=http://localhost:8080
